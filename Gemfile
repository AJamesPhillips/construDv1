source 'http://rubygems.org'

gem 'rails', '3.0.7'

# Bundle edge Rails instead:
# gem 'rails', :git => 'git://github.com/rails/rails.git'

gem 'sqlite3', '1.3.3'
gem 'pg', '0.11.0' #postgres database
gem 'gravatar_image_tag', '1.0.0.pre2'
gem 'will_paginate', '3.0.pre2'
gem 'rake', '0.8.7'  ##needed as newer, 0.9 version, of rake (as of 2011 May 22) breaks rails
gem "jquery-rails"
gem "nested_form"
gem 'acts-as-taggable-on'
gem 'composite_primary_keys', '=3.1.0'


group :development do
  gem 'rspec-rails', '2.5.0'
  gem 'annotate', '2.4.0'   ## run from command prompt with    bundle exec annotate --position before
  gem 'faker', '0.3.1'   ## for creating loads of random users to test the site with.
end


group :test do
  gem 'rspec', '2.5.0'
  gem 'webrat', '0.7.1'
  #gem 'spork', '0.9.0.rc5' ## see chapter 3 - page 17
  gem 'factory_girl_rails', '1.0'
end



# Use unicorn as the web server
# gem 'unicorn'

# Deploy with Capistrano
# gem 'capistrano'

# To use debugger (ruby-debug for Ruby 1.8.7+, ruby-debug19 for Ruby 1.9.2+)
# gem 'ruby-debug'
# gem 'ruby-debug19', :require => 'ruby-debug'

# Bundle the extra gems:
# gem 'bj'
# gem 'nokogiri'
# gem 'sqlite3-ruby', :require => 'sqlite3'
# gem 'aws-s3', :require => 'aws/s3'

# Bundle gems for the local environment. Make sure to
# put test-only gems in this group so their generators
# and rake tasks are available in development mode:
# group :development, :test do
#   gem 'webrat'
# end
